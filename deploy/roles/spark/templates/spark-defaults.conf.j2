
spark.master {{ spark_master_url }}

spark.default.parallelism {{ spark_default_parallelism }}

spark.jars.packages {{ spark_packages | join(',') }}

spark.driver.memory {{ spark_driver_memory }}
spark.driver.cores {{ spark_driver_cores }}

spark.executor.memory {{ spark_executor_memory }}

spark.local.dir {{ spark_local_dir }}

spark.task.maxFailures {{ spark_task_max_failures }}

# Don't write data to _temporary.
spark.hadoop.mapreduce.fileoutputcommitter.algorithm.version 2

# Avoid memory errors when reading large dfs.
spark.sql.parquet.enableVectorizedReader false

# more connections for parquet on s3a
spark.hadoop.fs.s3a.connection.maximum {{ spark_hadoop_fs_s3a_connection_maximum }}

spark.speculation {{ spark_speculation }}
spark.speculation.interval {{ spark_speculation_interval }}
spark.speculation.multiplier {{ spark_speculation_multiplier }}

spark.executor.heartbeatInterval {{ spark_executor_heartbeat_interval}}
spark.network.timeout {{ spark_network_timeout }}
